<Window x:Class="WeatherApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WeatherApp"
        mc:Ignorable="d"
        Title="" Height="720" Width="1280" ResizeMode="NoResize" 
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style BasedOn="{StaticResource {x:Type Label}}" 
               TargetType="Label"
               x:Key="dateYearLabels">
            <Setter Property="FontFamily" Value="Bebas Neue"/>
            <Setter Property="Foreground" Value="#B8C6C8"/>
        </Style>
    </Window.Resources>

    <Border CornerRadius="8" BorderThickness="0">
        <Border.BorderBrush>
            <SolidColorBrush Color="White"/>
        </Border.BorderBrush>
        <!--This is the foundational grid for the UI-->
        <Grid x:Name="BaseGrid">
            <!--This is the calendar Grid-->
            <Grid x:Name="dayDateGrid" Grid.Column="1" Width="Auto" Background="#FFFFFE">
                <Label Style="{StaticResource dateYearLabels}" Name="yearLabel" Content="2016" Width="Auto" Height="Auto" Grid.Column="0" Grid.Row="0" FontSize="30" Margin="10,50,0,0"  HorizontalContentAlignment="Center"/>
                <Label Style="{StaticResource dateYearLabels}" Name="dayLabel" Content="THURSDAY" Width="Auto" Height="Auto" Grid.Column="1" Grid.Row="0" Margin="420,35,0,0" FontSize="20" FontWeight="Normal" HorizontalContentAlignment="Center"/>
                <Label Style="{StaticResource dateYearLabels}" Name="monthDateLabel" Content="JANUARY 14" Width="Auto" Height="Auto" Grid.Column="1" Grid.Row="0" Margin="350,50,0,0" FontSize="30"  HorizontalContentAlignment="Center"/>
                
                <Grid x:Name="calendarGrid" Grid.Row="1">
                    <StackPanel Grid.Column="0" Grid.Row="0" Orientation="Horizontal" Margin="9,0,0,0" Grid.ColumnSpan="7">
                        <Image x:Name="leftArrow"/>
                        <Label Style="{StaticResource dateYearLabels}" x:Name="monthSelectionLabel" Content="January" FontWeight="Bold"/>
                        <Image x:Name="rightArrow"/>
                    </StackPanel>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/> <!--column 0-->
                        <ColumnDefinition Width="Auto"/> <!--column 1-->
                        <ColumnDefinition Width="Auto"/> <!--column 2-->
                        <ColumnDefinition Width="Auto"/> <!--column 2-->
                        <ColumnDefinition Width="Auto"/> <!--column 4-->
                        <ColumnDefinition Width="Auto"/> <!--column 5-->
                        <ColumnDefinition Width="Auto"/> <!--column 6-->
                        
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/> <!--Row 0-->
                        <RowDefinition Height="Auto"/> <!--Row 1-->
                        <RowDefinition Height="Auto"/> <!--Row 2-->
                        <RowDefinition Height="Auto"/> <!--Row 3-->
                        <RowDefinition Height="Auto"/> <!--Row 4-->
                        <RowDefinition Height="Auto"/> <!--Row 5-->
                        <RowDefinition Height="Auto"/> <!--Row 6-->
                    </Grid.RowDefinitions>
                </Grid>
                <!--Calendar grid end-->
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/> <!--column 0-->
                    <ColumnDefinition Width="Auto"/> <!--column 1-->
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!--row 0-->
                    <RowDefinition Height="Auto"/> <!--row 1-->
                </Grid.RowDefinitions>
                
            </Grid>
            <Border Name="mask" Background="White" CornerRadius="7"/>
            <!--The ViewBox is a very useful control in WPF. If does nothing more than scale to fit the content to the available size. It does not resize the content, but it transforms it. This means that also all text sizes and line widths were scaled. Its about the same behavior 
            as if you set the Stretch property on an Image or Path to Uniform.
            Although it can be used to fit any type of control, it's often used for 2D graphics, 
            or to fit a scalable part of a user interface into an screen area. -->
            <Viewbox Name="imageViewBox" Grid.Row="0" Grid.Column="0" Stretch="Fill">
                <Image x:Name="sunnyday" Visibility="Visible" />
            </Viewbox>

            <Viewbox Name="calendarViewBox" Grid.Row="0" Grid.Column="1" Stretch="Fill">
                
            </Viewbox>
            <!--Clipping mask used to give image rounded corners-->
            <Grid.OpacityMask>
                <VisualBrush Visual="{Binding ElementName=mask}"/>
            </Grid.OpacityMask>
            <Grid.Background>
                <SolidColorBrush Color="White"/>
            </Grid.Background>


            <Grid.RowDefinitions></Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            


        </Grid>
        <!--Foundation grid end-->
    </Border>
</Window>
