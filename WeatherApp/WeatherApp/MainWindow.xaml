<Window x:Class="WeatherApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WeatherApp"
        mc:Ignorable="d"
        Title="" Height="720" Width="1280" ResizeMode="NoResize" 
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style BasedOn="{StaticResource {x:Type Label}}" 
               TargetType="Label"
               x:Key="dateYearLabels">
            <Setter Property="FontFamily" Value="Bebas Neue"/>
            <Setter Property="Foreground" Value="#B8C6C8"/>
        </Style>
    </Window.Resources>

    <Border CornerRadius="8" BorderThickness="0">
        <Border.BorderBrush>
            <SolidColorBrush Color="White"/>
        </Border.BorderBrush>
        <!--This is the foundational grid for the UI-->
        <Grid x:Name="BaseGrid">
            <!--This is the calendar Grid-->
            <Grid x:Name="calendarGrid" Grid.Column="1" Grid.Row="2">
                <StackPanel Grid.Column="0" Grid.Row="0" Orientation="Horizontal" Width="Auto">
                    <Image x:Name="leftArrow"/>
                    <Label Style="{StaticResource dateYearLabels}" x:Name="monthSelectionLabel" Content="January" FontWeight="Bold"/>
                    <Image x:Name="rightArrow"/>
                </StackPanel>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <!--column 0-->
                    <ColumnDefinition Width="*"/>
                    <!--column 1-->
                    <ColumnDefinition Width="*"/>
                    <!--column 2-->
                    <ColumnDefinition Width="*"/>
                    <!--column 2-->
                    <ColumnDefinition Width="*"/>
                    <!--column 4-->
                    <ColumnDefinition Width="*"/>
                    <!--column 5-->
                    <ColumnDefinition Width="*"/>
                    <!--column 6-->

                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <!--Row 0-->
                    <RowDefinition Height="*"/>
                    <!--Row 1-->
                    <RowDefinition Height="*"/>
                    <!--Row 2-->
                    <RowDefinition Height="*"/>
                    <!--Row 3-->
                    <RowDefinition Height="Auto"/>
                    <!--Row 4-->
                    <RowDefinition Height="Auto"/>
                    <!--Row 5-->
                    <RowDefinition Height="Auto"/>
                    <!--Row 6-->
                </Grid.RowDefinitions>
            </Grid>
            <!--Calendar grid end-->
            <Border Name="mask" Background="White" CornerRadius="7"/>
            <!--The ViewBox is a very useful control in WPF. If does nothing more than scale to fit the content to the available size. It does not resize the content, but it transforms it. This means that also all text sizes and line widths were scaled. Its about the same behavior 
            as if you set the Stretch property on an Image or Path to Uniform.
            Although it can be used to fit any type of control, it's often used for 2D graphics, 
            or to fit a scalable part of a user interface into an screen area. -->
            <Viewbox Name="imageViewBox" Grid.Row="0" Grid.Column="0" Stretch="UniformToFill">
                <Image x:Name="sunnyday" Visibility="Visible" />
            </Viewbox>
            <Label Style="{StaticResource dateYearLabels}" Name="yearLabel" Content="2016" Width="Auto" Height="Auto" Grid.Column="1" Grid.Row="0" Grid.RowSpan="2" FontSize="30" Margin="0,50,550,0"  HorizontalContentAlignment="Center"/>
            <Label Style="{StaticResource dateYearLabels}" Name="dayLabel" Content="THURSDAY" Width="Auto" Height="Auto" Grid.Column="1" Grid.Row="0" Margin="0,35,30,0" FontSize="20" FontWeight="Normal" HorizontalContentAlignment="Right" Foreground="#C7D3D5"/>
            <Label Style="{StaticResource dateYearLabels}" Name="monthDateLabel" Content="MARCH 14" Width="Auto" Height="Auto" Grid.Column="1" Grid.Row="0" Margin="0,50,30,0" FontSize="30"  HorizontalContentAlignment="Right"/>
            <!--Clipping mask used to give image rounded corners-->
            <Grid.OpacityMask>
                <VisualBrush Visual="{Binding ElementName=mask}"/>
            </Grid.OpacityMask>
            <Grid.Background>
                <SolidColorBrush Color="White"/>
            </Grid.Background>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            
        </Grid>
        <!--Foundation grid end-->
    </Border>
</Window>
